describe('MessagesV2', () => {

    it('Create actions', () => {
        // const testRequest = createRequest<string, number>("test");
        // const testAction = createFromRequest<string, number>(testRequest, (payload) => {
        //     return Promise.resolve(payload.length);
        // });
        // const testAction2 = createAction<string, number>("test2", (payload) => {
        //     return Promise.resolve(payload.length);
        // });
        // const messages = new MessagesV2(true);
        // messages.listen(testAction);
        // messages.listen(testAction2);
        // messages.request(testRequest("TEST")).finally();
    });
});