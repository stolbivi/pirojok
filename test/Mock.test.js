describe('MessagesV2', () => {

    it('Create actions', () => {
        // const testRequest = createRequest<string, number>("test");
        // const testAction = createFromRequest<string, number>(testRequest, (payload, sender) => {
        //     console.log(sender?.tab);
        //     return Promise.resolve(payload.length);
        // });
        // const messages = new MessagesV2(true);
        // messages.listen(testAction);
        // messages.request(testRequest("TEST")).finally();
    });
});